# Sellomize Reach local secrets configuration file
# This file satisfies Streamlit runtime checks when run locally or frozen.
# Keys are also managed persistently in Tab 5 (Settings) stored in email_system.db.
